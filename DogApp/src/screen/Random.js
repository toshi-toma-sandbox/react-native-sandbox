/**
 * @format
 * @flow strict-local
 */

import React from 'react';
import RandomList from '../components/RandomList';

const RandomScreen: () => React$Node = () => {
  return <RandomList />;
};

export default {
  name: 'RandomScreen',
  component: RandomScreen,
};
